import React from 'react';
import { Container, Typography } from '@material-ui/core';

const Dashboard = () => {
  return (
    <Container>
      <Typography variant='h4'>Hello From Dashboard</Typography>
    </Container>
  );
};

export default Dashboard;
